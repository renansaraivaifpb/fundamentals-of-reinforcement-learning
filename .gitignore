# --- Python ---
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/

# --- Artefatos de experimento (NÃO versionar em Git) ---
# Estes diretórios já contêm arquivos rastreados historicamente e, por ora, são o
# único backup dos modelos treinados — por isso NÃO foram desrastreados.
# Este ignore vale para execuções FUTURAS. A solução definitiva é DVC ou
# MLflow artifacts: Git não é feito para binários nem para JSONs de 120k linhas.
sb3_logs*/
rl_analysis_logs/
results_*/
mlruns/

# --- Saídas de análise ---
# `plots_analysis_*/` deliberadamente NÃO está aqui: os dashboards são figuras
# candidatas ao paper e você já versiona um deles de propósito.
*.png.tmp

# --- Sistema operacional ---
.DS_Store
Thumbs.db

# --- Artefatos da reprodução v4/paper ---
# TensorBoard: regenerável a partir do treino.
logs_paper/
logs_ablation/
v4/paper/logs_*/

# Configuração local do editor.
.vscode/

# PDFs de terceiros (Elsevier/Springer/Nature): material protegido por direito
# autoral, não redistribuível. Referenciar por DOI no lugar.
papers_sugeridos/

# Exceção: os CSVs da ablação e do tabular são RESULTADOS experimentais
# (não artefatos regeneráveis) — o padrão `results_*/` acima os pegaria.
!v4/paper/results_ablation/
!v4/paper/results_ablation/**

# Modelos SAC/TD3 sao ~6 MB cada (ator + 2 criticos + targets). Os canonicos
# (models_lab3) sao versionados; os intermediarios superados ficam so em disco.
# Sao regeneraveis: `python train.py --lab2 --seeds 0 1 2` reproduz exatamente,
# porque model_io.py garante que a config de treino esta no metadado.
v4/paper/models_lab2/
v4/paper/logs_lab*/
v4/paper/logs_obs_ablacao/

# --- Pacote de submissão e manuscritos gerados ---
# Tudo abaixo é SAÍDA dos geradores em v5/ (gerar_paper.py, gerar_paper_eb.py,
# gerar_cover_letter.py). Versionar o .docx ao lado do código que o produz cria
# duas fontes de verdade e um conflito binário a cada regeração; o que precisa
# ser revisto é o gerador. Reproduza com:
#     cd v5 && python gerar_paper_eb.py && python gerar_cover_letter.py
v5/submissao_eb/
v5/paper_auditoria_hvac_rl.docx
v5/paper_auditoria_hvac_rl_curto.docx
paper_auditoria_hvac_rl.pdf

# Classe LaTeX CAS da Elsevier: material de terceiros, redistribuído pela
# própria editora e baixável de elsevier.com/authors/policies-and-guidelines/
# latex-instructions. `gerar_paper_eb.py --tex` copia os arquivos de classe
# para junto do .tex a partir deste diretório.
els-cas-templates.zip
els-cas-templates/

# Template .dot da MDPI Energies: mesma categoria, material da editora.
energies-template.dot

# Manuscrito auditado: PDF não publicado, mantido apenas em disco.
29914_Paper_manuscript.pdf
